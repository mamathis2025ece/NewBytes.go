# Benchmark Methodology

Benchmarking can be performed using Go's built-in testing framework.

Command:

go test -bench=.

The benchmarks measure execution time for parsing and formatting byte values.
